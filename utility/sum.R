sumRows <- function(dataset){
  # result <- data.frame(matrix(ncol = 1, nrow = 0))
  # for(row in 1:nrow(dataset)){
  #   print(dataset[row,-3]);
  #   sumR <- sum(dataset[row,-3]);
  #   result <- rbind(result,sumR);
  # }
  # return(result);
  print(nrow(dataset));
}

#a <- data.frame(matrix(c(1:6),nrow = 3,ncol = 2))
#b <- data.frame(matrix(c(7:12),nrow = 3,ncol = 2))
#a <- cbind(a,X3 = c("A","B","C"))
#b <- cbind(b,X3 = c("D","E","F"))
